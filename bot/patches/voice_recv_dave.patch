--- a/discord/ext/voice_recv/reader.py
+++ b/discord/ext/voice_recv/reader.py
@@ -113,7 +113,19 @@ class AudioReader:
             packet_data: bytes) -> None:
         packet = rtp_packet = rtcp_packet = None
         try:
             if not rtp.is_rtcp(packet_data):
                 packet = rtp_packet = rtp.decode_rtp(packet_data)
-                packet.decrypted_data = self.decryptor.decrypt_rtp(packet)
+                # First decrypt transport layer (RTP encryption)
+                transport_decrypted = self.decryptor.decrypt_rtp(packet)
+
+                # Then decrypt DAVE E2EE frame encryption if session exists
+                if (hasattr(self.voice_client, '_connection') and
+                    hasattr(self.voice_client._connection, 'dave_session') and
+                    self.voice_client._connection.dave_session is not None):
+                    try:
+                        packet.decrypted_data = self.voice_client._connection.dave_session.decrypt_opus(
+                            packet.ssrc, transport_decrypted
+                        )
+                    except Exception as e:
+                        log.warning("DAVE decryption failed for SSRC %s: %s", packet.ssrc, e)
+                        packet.decrypted_data = transport_decrypted  # Fallback to transport-only
+                else:
+                    packet.decrypted_data = transport_decrypted
             else:
                 packet = rtcp_packet = rtp.decode_rtcp(self.decryptor.decrypt_rtcp(packet_data))
